import java.util.*;

public class Practice3 {

    public static void main(String[] args) {

        Scanner input = new Scanner(System.in);
        // for(int i = 0; i < 50; i++) {
        //     System.out.print("*");
        //     if((i + 1) % 5 == 0){
        //         System.out.println();
        //     }
        // }

        // System.out.print("Enter how much money you have: ");
        // double amt = input.nextDouble();

        // int dollars = (int) amt / 1;
        // double quarters = amt - dollars / .25;
        // double dimes = (amt - dollars) - (quarters * .25) / .10;
        // double nickles = (amt - dollars) - (quarters * .25) - (dimes * .10) / .5;
        // double pennies = (amt - dollars) - (quarters * .25) - (dimes * .10) - (nickles * .5) / .01;

        // System.out.println("You have " + dollars + " dollars, " + quarters + " quarters, " + dimes + " dimes, " + nickles + " nickles, and " + pennies + " pennies!");
        
        
    }
}